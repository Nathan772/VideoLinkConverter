retourner à un ancien commit : 

git checkout  87acccafff1a3ac12dbd908d652dfb29be80f84b

Résoudres les pbs du type :

"not a managed type"

"parameter 0 of constructor..."

--> lié à la hiérarchie des éléments, difficulté pour la machine de trouver les éléments
dans l'arborescence.
Essayer d'ajouter le path ou de mettre les éléments que ccherche le repository dans le même package
ou sous package, tout en rendant l'application (Application.java) parent de ces package

résoudre :
"Failed to execute goal org.springframework.boot:spring-boot-maven-plugin"


 https://stackoverflow.com/questions/45400701/failed-to-execute-goal-org-springframework-bootspring-boot-maven-plugin1-5-6-r
			
    	ajout lié à cette page (début) :
		
				 permet de résoudre : 
				<configuration>
					<skip>true</skip>
				</configuration>
				<executions>
					<execution>
						<phase>none</phase>
					</execution>
				</executions>


		

curl --location 'http://localhost:8080/users' \ --header 'Content-Type: application/json' \ --data-raw '{"name": "John Doe", "email": "john.doe@baeldgung.com"}'

Pour toutes les commandes npm install ou yarn install, il faut les lancer à la racine de la partie react pour qu'elles fonctionnent : 
dans mon cas, c'est dans le dossier "reactjs".
Par contre, pour lancer la commande qui lance projet, c'est dans le dossier où est situé le fichier "pom.xml".